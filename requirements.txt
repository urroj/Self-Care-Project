# ML pipeline
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0
scipy>=1.11.0
statsmodels>=0.14.0
xgboost>=2.0.0
joblib>=1.3.0
pyarrow>=14.0.0

# Deep learning (optional — only needed at Phase 3, 8+ personal cycles)
torch>=2.2.0

# Database
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0

# Web API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Utilities
pydantic>=2.5.0
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0

# ── Desktop window (optional) ─────────────────────────────────────────────────
# pywebview opens a native OS window around the web app.
# If installation fails (common on Windows due to pythonnet build issues),
# skip it — the app falls back to opening in your default browser automatically.
#
# To install manually after the rest:
#   pip install pywebview --no-deps
#
# pywebview>=5.0.5